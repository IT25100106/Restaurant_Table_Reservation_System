# Server
server.port=8080

# Data folder path — relative to working directory (fixed/)
# Points up one level to ResturantBackend_Fixed/data/ where seed files live
app.data.path=../data

# CORS
app.cors.allowed-origins=http://localhost:3000

# Logging
logging.level.com.restaurant=DEBUG
